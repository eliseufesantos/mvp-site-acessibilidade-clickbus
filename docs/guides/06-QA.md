# QA e validação

Data da última validação: 09/09/2026. Relatório detalhado: [`../accessibility-agent/VALIDATION.md`](../accessibility-agent/VALIDATION.md).

## Ambientes e comandos

- Node `v22.22.0`; pnpm `11.12.0`;
- navegador integrado Codex: desktop 1265×711 e mobile 378×629;
- Chrome headless/CDP: viewport CSS exato de 320×844, capturado pelo script reproduzível do projeto;
- build/preview Vite local;
- `tsc --noEmit -p tsconfig.app.json`: PASS;
- `vite build --configLoader runner`: PASS, 1.614 módulos;
- `node scripts/run-accessibility-tests.mjs`: PASS, 11 testes.

Os scripts via `pnpm` tentaram reconciliar `node_modules` sem TTY/rede e abortaram antes de executar. Por isso a validação registrou também as chamadas diretas aos binários locais.

## Casos aprovados nesta rodada

- painel desktop não modal e mobile com semântica de diálogo;
- nenhum overflow horizontal em 1265 px, 378 px e 320 px;
- em 320×844: `innerWidth=clientWidth=scrollWidth=320`, painel aberto com 320 px e acionador dentro do viewport;
- aba Conteúdo volta ao topo após troca (`scrollTop=0`);
- falha do planejador retorna 503 finito e mantém controles manuais;
- contraste e texto 150% aplicam sem zoom de página;
- letras e entrelinha funcionam de forma independente;
- desfazer restaura apenas a última alteração; restauração volta ao padrão;
- preferências sobrevivem ao recarregamento;
- guia e máscara coexistem e usam `pointer-events: none`;
- glossário explica “viação” localmente;
- simplificação sem provedor informa indisponibilidade e preserva original;
- bloco Rybená mostra atribuição e estado técnico pendente;
- DOM e recursos observados contêm zero scripts/requisições Rybená/VLibras;
- conteúdo autorizado exclui checkout e confirmação;
- typecheck, build e 11 testes do core/servidor passam.

## Não executado ou bloqueado

- modelo real: **NOT RUN — provider configuration missing**;
- Rybená real: **BLOCKED / NOT VALIDATED — provider API not yet released/configured**;
- microfone: fluxo implementado, permissão não concedida nesta rodada;
- NVDA/VoiceOver e teste humano: pendentes;
- tradução avaliada por pessoas surdas sinalizantes: pendente;
- Safari/iOS real e zoom 200%: pendentes;
- jornada completa repetida após esta evolução: pendente; regras comerciais não foram alteradas.

Não alegar conformidade integral com WCAG nem funcionamento real de IA/Rybená a partir desta rodada.
